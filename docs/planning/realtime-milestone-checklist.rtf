{\\rtf1\\ansi\\deff0 {\\fonttbl {\\f0 Calibri;}} \\fs22 Sprint To-Do List (Milestone by Milestone)\\par
\\par
Milestone 1 (Weeks 1-2): Contract Freeze + Ingress Hardening\\par
- [ ] Define canonical event envelope fields and required/optional matrix in team notes.\\par
- [ ] Add runtime validation helper usage in ingress path (socket + HTTP) behind flag.\\par
- [ ] Add idempotency key policy (eventId preferred, fallback correlationId+type+scope+scopeId).\\par
- [ ] Add explicit error mapping table for invalid_event, duplicate_event, forbidden_scope, unauthorized.\\par
- [ ] Keep existing response aliases (suggestions, suggestion) intact for UI compatibility.\\par
\\par
Tests to add in _tests_/realtimeCoordinator.test.js\\par
- [ ] ingest rejects signal missing required envelope fields\\par
- [ ] ingest accepts valid canonical envelope and preserves eventId/correlationId\\par
- [ ] ingest deduplicates duplicate eventId within dedupe window\\par
- [ ] ingest does not dedupe different eventId with same payload\\par
- [ ] ingest normalizes scope defaults to household when absent\\par
- [ ] ingest routes family-scoped signal to family queue only\\par
- [ ] audit history records validation failures with reason code\\par
- [ ] state counters include droppedDuplicate and droppedInvalid totals\\par
\\par
Tests to add in _tests_/realtimeController.runtime.contract.test.js\\par
- [ ] POST /api/realtime/signals returns 400 invalid_event for malformed payload\\par
- [ ] POST /api/realtime/signals returns 200 and includes signal id for valid payload\\par
- [ ] POST /api/realtime/signals returns 409 duplicate_event for repeated eventId\\par
- [ ] GET /api/realtime/suggestions still returns both items and suggestions aliases\\par
- [ ] cross-scope household query without elevated role returns 403 forbidden_scope\\par
- [ ] family scope request without familyId returns 403 family_scope_forbidden\\par
- [ ] household scope request without homeId returns 403 household_scope_missing\\par
\\par
Milestone 2 (Weeks 3-4): Durable Event Log + Replay\\par
- [ ] Introduce append-only log abstraction and wire ingress writes before projection.\\par
- [ ] Add boot-time replay path to rebuild coordinator read models.\\par
- [ ] Add replay checkpoint marker and replay idempotency guard.\\par
- [ ] Keep in-memory path as fallback behind feature flag for rollback safety.\\par
\\par
Tests to add in _tests_/realtimeCoordinator.test.js\\par
- [ ] replay rebuilds suggestion queue from persisted events in order\\par
- [ ] replay is idempotent when same event batch is applied twice\\par
- [ ] replay preserves consumed and assigned suggestion state transitions\\par
- [ ] replay restores latest report summary fields correctly\\par
- [ ] out-of-order events produce deterministic final queue ordering\\par
- [ ] expired suggestions remain pruned after replay\\par
- [ ] signal history cap still enforced during replay\\par
\\par
Tests to add in _tests_/realtimeController.runtime.contract.test.js\\par
- [ ] POST /signals appends event before coordinator mutation (observable via stub call order)\\par
- [ ] service restart simulation + replay restores /suggestions response consistency\\par
- [ ] service restart simulation + replay restores /reports/latest content\\par
- [ ] append store failure returns 503 with stable error contract\\par
- [ ] controller remains healthy when replay is disabled by feature flag\\par
\\par
Milestone 3 (Weeks 5-6): Projection Split + Readiness SLIs\\par
- [ ] Split ingest and projection responsibilities (coordinator becomes projector/read model).\\par
- [ ] Add readiness metrics counters and latency timing around ingest-to-queue update.\\par
- [ ] Add backlog/staleness calculation for high-priority unassigned suggestions.\\par
- [ ] Expose lightweight diagnostics endpoint or payload fields for test visibility.\\par
\\par
Tests to add in _tests_/realtimeCoordinator.test.js\\par
- [ ] projector updates queueDepth metric after each ingest\\par
- [ ] projector increments highPriorityPending for priority >= 80\\par
- [ ] assignSuggestion decreases unassignedPending and increases assignedPending\\par
- [ ] consumeSuggestion decreases pendingSuggestions and increases completedSuggestions\\par
- [ ] report generation includes signals24h and signalBreakdown accuracy\\par
- [ ] latency metric recorded for ingest-to-queue-update path\\par
- [ ] projector handles burst ingest without duplicate queue items\\par
\\par
Tests to add in _tests_/realtimeController.runtime.contract.test.js\\par
- [ ] GET /reports/latest includes readiness summary keys (pending, assigned, unassigned, highPriority)\\par
- [ ] POST /suggestions/:id/assign updates subsequent /suggestions listing\\par
- [ ] POST /suggestions/:id/consume updates subsequent /reports/latest summary\\par
- [ ] GET /audit returns bounded list honoring limit query param\\par
- [ ] GET /signals returns bounded list honoring limit query param\\par
- [ ] CSV export endpoint keeps required columns after projection refactor\\par
\\par
Milestone 4 (Weeks 7-8): Neo4j Graph Projection (Non-Blocking)\\par
- [ ] Add async graph projector subscriber from event log.\\par
- [ ] Keep core realtime behavior independent of Neo4j availability.\\par
- [ ] Add graph projection retry queue and dead-letter counter.\\par
- [ ] Add graph-enriched report field as optional extension, not required contract.\\par
\\par
Tests to add in _tests_/realtimeCoordinator.test.js\\par
- [ ] graph projection hook receives normalized relationship payload per signal\\par
- [ ] graph projection failure does not block suggestion creation\\par
- [ ] graph projection retry increments retry counter and preserves event\\par
- [ ] graph enrichment data merges into report without mutating base summary\\par
- [ ] coordinator behavior unchanged when graph projector disabled\\par
\\par
Tests to add in _tests_/realtimeController.runtime.contract.test.js\\par
- [ ] core endpoints remain 200 when graph service unavailable\\par
- [ ] optional graph fields absent or null without breaking base contract\\par
- [ ] optional graph fields present when graph projection enabled\\par
- [ ] scope authorization still enforced for graph-augmented report requests\\par
\\par
Definition of Done (Every Milestone)\\par
- [ ] New tests are deterministic and do not depend on wall-clock timing without fakes.\\par
- [ ] Feature flags have both enabled and disabled path coverage.\\par
- [ ] Existing compatibility aliases and status codes remain unchanged unless intentionally versioned.\\par
- [ ] No regression in existing tests for suggestions/report contracts.\\par
- [ ] Add one negative-path runtime contract test per new error code introduced. }
