import {
  vi,
  describe,
  it,
  test,
  expect,
  beforeAll,
  beforeEach,
  afterAll,
  afterEach,
} from "vitest";

export const jest = vi;
export { describe, it, test, expect, beforeAll, beforeEach, afterAll, afterEach };
export default { jest, describe, it, test, expect, beforeAll, beforeEach, afterAll, afterEach };
